#include "main.h"
/**
 * main - Entry point
 * Description: Print _isalpha
 *Return: Always 0 (Success)
 */
int _isalpha(int c)
{
if ((c > 'a' && c < 'z') || (c > 'A' && c < 'Z'))
{return (01);
}
else 
{return (0);
}
}
